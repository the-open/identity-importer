require 'spec_helper'

describe Identity::Importer do
  it 'has a version number' do
    expect(Identity::Importer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
